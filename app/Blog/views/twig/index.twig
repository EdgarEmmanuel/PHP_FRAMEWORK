{% extends "header.html" %}

{% block content %}
    <h1>WELCOME HERE</h1>

    <ul>
        <li>
            <a href="{{  path('blog.show', {slug: 'edgar'}) }}">
                Article Edgar
            </a>
        </li>
        {% for post in posts %}
            <li>
                <a href="{{ path('blog.show', {slug: post.slug, id:post.id}) }}">
                    {{ post.name }}
                </a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}